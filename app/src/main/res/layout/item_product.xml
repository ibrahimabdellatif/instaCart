<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"

    android:layout_margin="2dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_product_item"
        android:layout_width="120dp"
        android:layout_height="110dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="3dp"
        tools:ignore="MissingConstraints">

        <ImageView
            android:id="@+id/iv_product"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="15dp"
            android:elevation="2dp"
            android:scaleType="fitXY"
            android:src="@drawable/banana" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_product_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        app:layout_constraintEnd_toEndOf="@id/cv_product_item"
        app:layout_constraintStart_toStartOf="@id/cv_product_item"
        app:layout_constraintTop_toBottomOf="@id/cv_product_item"
        android:layout_margin="5dp"
        android:maxLines="1"
        android:text="Organic Banana (each)"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:textSize="12sp"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/tv_product_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_product_name"
        app:layout_constraintStart_toStartOf="@id/tv_product_name"
        app:layout_constraintTop_toBottomOf="@id/tv_product_name"
        android:text="$15.00"
        android:textColor="#2be01f"
        android:textSize="12sp"
        tools:ignore="MissingConstraints" />


</androidx.constraintlayout.widget.ConstraintLayout>